# Runtime Data

This directory is reserved for local user-owned runtime data.

- `sqlite/`: structured memory database files.
- `chroma/`: local vector database files.
- `backups/`: local backups.
- `exports/`: user-controlled exports.

Runtime data should not be committed to source control.

